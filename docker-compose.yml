services:
  web:
    image: ${WEB_IMAGE:-michaelschultzcom-web}
    build: .
    cpus: 0.8
    restart: unless-stopped
    volumes:
      - listening-data:/app/data
    env_file:
      - .env
    networks:
      - pangolin

volumes:
  listening-data:

networks:
  pangolin:
    external: true
