services:
  web:
    build: .
    image: michaelschultzcom-web
    restart: unless-stopped
    volumes:
      - listening-data:/app/data
    env_file:
      - .env
    networks:
      - pangolin

volumes:
  listening-data:

networks:
  pangolin:
    external: true
